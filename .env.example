# Encryption password for diary entries
# 本地测试可用: test-password-123
# 生产环境请使用强密码，可通过以下命令生成:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_PASSWORD=test-password-123
