# 2026-07-22

## diary-blog 模板选型讨论
- 用户反馈当前首页「开门见山」：一打开就是解密日记表单，希望后续也能发布非日记内容。
- 项目基于 Astro 4.16，依赖非常精简（仅 astro + @notionhq/client + tsx/dotenv）。
- 已推荐 Astro 官方主题站及若干轻量博客模板（AstroPaper、Nano、Astro Ink、Blogster Minimal、Gumori、Chiri、Bookworm Light）。
- 待用户选定模板后可协助迁移/整合现有日记数据与加密功能。

## 选定 Fuwari 并套用到项目
- 用户选定 **Fuwari**（saicaca/fuwari）主题。实测：源码~2MB，node_modules~533MB（含 Svelte/Tailwind/KaTeX/Pagefind/Sharp/PhotoSwipe），dist~5MB。
- 已完成迁移：清空旧 src 复制 Fuwari 基础，合并 public（保留 public/diaries）、scripts（保留 encrypt.ts/sync-notion.ts + Fuwari 的 new-post.js）；恢复 crypto.ts / diary.ts。
- 重写日记组件为 Fuwari 同款 CSS 变量风格：DecryptForm / DiaryDecryptor / DiaryCard / DiaryLayout（包裹 MainGridLayout）。
- 新建页面：/diary 列表页（按年分组 + 标签云 + 随机回顾按钮 + 标签筛选 JS）、/diary/[slug] 详情页（客户端解密，markdown-it 渲染）。
- 配置：base='/diary-blog'、site URL、src/config.ts 中文站点名+lang=zh_CN、navBarConfig 增加「日记」内部链接；更新 About 页与新增 welcome 中文文章；删除 Fuwari demo posts。
- package.json：保留 Fuwari 依赖并重新加回 @notionhq/client、tsx、dotenv 等；移除 preinstall only-allow pnpm 限制以便用 npm。
- 验证：`npm run dev` 正常（首页/日记页均 HTTP 200），首页无解密框、导航有「日记」入口、列表卡片/标签/详情解密表单均正确。
- 注意：`npm run build` 在本沙箱内会因「安全删除保护」拦截 Astro 清理 server chunks 而退出码 1（仅沙箱限制，用户本机不受影响）；pagefind 索引已手动补跑。预览建议用 `npm run dev`（base 路径下访问 http://localhost:4321/diary-blog/）。
- 备份位置：/tmp/diary-blog-backup（含迁移前 src/scripts/public/diaries/package.json/astro.config.mjs）。
