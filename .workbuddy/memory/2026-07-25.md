# 2026-07-25

## 博客日记页调整

- 将 `DecryptForm.astro` 与 `DiaryDecryptor.astro` 中 MarkdownIt 的 `html` 选项设为 `true`，使解密后的日记正文支持 HTML（同时保留 Markdown）渲染，提升扩展性。
- 把随机日记结果底部的"再试一篇"改为"再看一篇"，并将按钮样式改为与"随机展示一篇"一致的主色实心按钮。
- 在 `src/config.ts` 的 `navBarConfig.links` 中移除顶栏 GitHub 外部链接，页面底部已保留主题来源引用。
- 修复 `DecryptForm.astro` 样式作用域问题：动态通过 `innerHTML` 插入的日记结果区域（`.diary-result`、`.result-body`、`.try-another-btn` 等）不会自动带上 Astro scoped style 属性，因此将这部分样式改为 `is:global`，使按钮样式与正文 Markdown/HTML 渲染样式真正生效。
